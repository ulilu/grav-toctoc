<ul class="langswitcher">
{% set langobj = grav['language'] %}

{% for key, language in langswitcher.languages %}

    {% if key == langswitcher.current %}
        {% set lang_url = page.url %}
        {% set active_class = 'active' %}
    {% else %}
        {% set lang_url = base_url_simple ~ langobj.getLanguageURLPrefix(key)~langswitcher.page_route ?: '/' %}
        {% set active_class = '' %}
    {% endif %}

    <li><a href="{{ lang_url }}" class="full-lang {{ active_class }}">{{  language.nativeName|capitalize }}</a><a href="{{ lang_url }}" class="short-lang {{ active_class }}">{{  language.nativeName|upper|slice(0,2) }}</a></li>

{% endfor %}
</ul>
