<ul class="instagram-photos">
{% for post in feed|slice(0, count)  %}
    <li>{{ page.media[post.image].lightbox(600).cropResize(200).html }}
 {#    	<a rel="lightbox" href="{{ post.link }}" target="_blank"><img src="{{ post.image }}" alt=""></a> #}
{% endfor %}
</ul>
