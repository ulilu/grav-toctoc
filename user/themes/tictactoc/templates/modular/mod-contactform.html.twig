{% set page = page.find('/contact', true) %}

<div class="block  block--stretch  block--contactform">
    <div class="block">
        <h1>{{ page.header.formtitle }}</h1>
        {% include "forms/form.html.twig" %}
    </div>
</div>