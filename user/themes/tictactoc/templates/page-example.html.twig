{% extends 'partials/base.html.twig' %}

{% block content %}

<div class="stage">
    <h1 class="gamma">{{ page.title }}</h1>
    {{ page.media[page.header.image].html(page.title, page.title ,page.header.imagealign) }}
    {{ page.media[page.header.image] }}
</div>

<div class="block  block--example">
    {{ page.content }}
</div>

{% endblock %}