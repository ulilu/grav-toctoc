{% extends 'partials/base.html.twig' %}

{% block content %}

<div class="stage">
    <h1 class="gamma">{{ page.title }}</h1>
    <div class="stage__img">{{ page.media[page.header.image].html(page.title, page.title ,page.header.imagealign) }}</div>
</div>

<div class="block  block--example">
    {{ page.content }}
</div>

{% endblock %}